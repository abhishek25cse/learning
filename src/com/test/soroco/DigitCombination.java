package com.test.soroco;

import java.util.ArrayList;
import java.util.HashMap;
import java.util.List;

/*
mappings:
1 -> abc
2 -> def
3 -> ghi
4 -> jkl

Task: In the given input (digits), replace each digit with ONE of the characters it is mapped to. For example in the first sample input "23" (see below),
2 is to be replaced with 'd', 'e' or 'f', similarly 3 should be replaced with 'g', 'h', or 'i'. The task is to output ALL such
strings formed by replacing like this, preferably in the order the characters appear in the mappings.

input: 23
output: dg dh di eg eh ei fg fh fi

input: 123
output: adg adh adi bdg bdh bdi cdg...*/
public class DigitCombination {
	
	/*public List<Integer> recursion(Node n ) {
		if(n.next() == null) {
			return n
		} else {
			
			return recurion(N.next());
		}
	}*/
	

	/*
	 * public static void main(String[] args) {
	 * 
	 * HashMap<Integer , String> intLetterMap = new HashMap<Integer, String>();
	 * intLetterMap.put(1, "abc"); intLetterMap.put(2, "def"); intLetterMap.put(3,
	 * "ghi"); intLetterMap.put(4, "jkl");
	 * 
	 * }
	 * 
	 * public static List<String> getCombination(HashMap<Integer , String>
	 * intLetterMap , Integer input) { List<String> finalList = new
	 * ArrayList<String>(); String inputStr = ""+input;
	 * 
	 * 
	 * 
	 * 
	 * 
	 * return finalList; }
	 */
}
